module adder (    
    input alufn[6], //alufn
    input a[16],
    input b[16],
    
    output out[16]
  ) {

  always {
  // there *might* be an issue here
    if (alufn == 0) {
      // perform addition
      out = a + b;
    }
}
}
