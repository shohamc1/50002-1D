module adder (    
    input alufn[2], //alufn
    input a[7],
    input b[7],
    
    output out[7]
  ) {

  always {
  // there *might* be an issue here
    if (alufn == 0) {
      // perform addition
      out = a + b;
    }
}
}
