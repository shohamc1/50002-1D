module multiplier (
    input a[16],
    input b[16],
    input alufn[6], //alufn
    output out[16]
  ) {

  always {
    out = 0;
    
    case(alufn) {
      2: out = a * b; // multiplication
      //3: out = a / b; // division
    }
  }
}
